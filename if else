#include<stdio.h>
int main(){
    int num;
    printf("\n Enter num:");
    scanf("\n%d",&num);
    if(num>=0)
    {
        printf("\n positive num");
    }
    else
    {
        printf("\n negative num");
    }
return 0;
}
